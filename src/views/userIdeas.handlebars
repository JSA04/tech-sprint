<h2 class="mb-3">Perfil do Usuário</h2>

<div class="card mb-4 shadow-sm">
  <div class="card-body">
    <h4 class="card-title">{{user.name}}</h4>
    <p class="card-text mb-0"><strong>Email:</strong> {{user.email}}</p>
  </div>
</div>

<h3 class="mb-3">Ideias Criadas</h3>

{{#if ideas.length}}
  <div class="row g-3">
    {{#each ideas}}
      <div class="col-12 col-sm-6 col-lg-4">
        <div class="card h-100 shadow-sm">
          <div class="card-body d-flex flex-column">
            <div class="d-flex justify-content-between">
              <h5 class="card-title mb-2">{{this.title}}</h5>
              <span>{{this.voteScore}} pontos de votos</span>
            </div>
            <div class="mb-2 text-muted">
              <span class="badge text-bg-secondary">{{this.category.name}}</span>
            </div>
            <p class="card-text text-truncate-3">{{this.description}}</p>
            <div class="small text-muted">
              Criada em {{formatDate this.createdAt}}
            </div>

            <!-- Ações -->
            <h6 class="text-secondary fw-bold mt-2">Ações</h6>
            <div class="d-flex flex-nowrap gap-2 align-items-center">
              <a href="/ideas/{{this.id}}" class="btn btn-primary btn-sm">👁️</a>
              <a href="/ideas/{{this.id}}/edit" class="btn btn-warning btn-sm">✏️</a>
              <form class="d-inline-block m-0 flex-shrink-0" action="/ideas/{{this.id}}/delete" method="POST">
                <input type="hidden" name="_csrf" value="{{../csrfToken}}">
                <button type="submit" class="btn btn-danger btn-sm">🗑️</button>
              </form>
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
{{else}}
  <div class="empty-state">
    <p>😎 Nenhuma ideia criada ainda. Bora começar?</p>
    <a href="/ideas/new" class="btn btn-primary">Criar Primeira Ideia</a>
  </div>
{{/if}}
